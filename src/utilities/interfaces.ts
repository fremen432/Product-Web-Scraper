export interface Coordinates {
    x: number;
    y: number;
    height: number;
    width: number;
}

export interface Inputs {
    webSite: String,
    productName: String,
    min_price: Number,
    max_price: Number,
    condition: String
}
